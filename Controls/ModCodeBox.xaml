<!-- ============================================================================
  ApolloGUI — ModCodeBox.xaml
  Purpose: TODO: brief purpose of this file.
  Key UI elements: TODO: key types/classes used.
  Notes: TODO: important usage and gotchas.
  Version: v1.0.0   Date: 2025-08-31
  Copyright (c) 2025 Skiller S
  ============================================================================
  Change Log:
    - v1.0.0 (2025-08-31): Repository-ready header added.
============================================================================ -->

<UserControl x:Class="ApolloGUI.ModCodeBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Name="root"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <UserControl.Resources>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#0C000000"/>
        <SolidColorBrush x:Key="CardBorderBrush" Color="#22000000"/>
        <SolidColorBrush x:Key="HeaderBrush" Color="#DD000000"/>

        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style TargetType="DataGrid" x:Key="CodeGridStyle">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="28"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="AlternatingRowBackground" Value="#06000000"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="HeaderText">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource HeaderBrush}"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="SubtleText">
            <Setter Property="Opacity" Value="0.75"/>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource CardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="2,0,2,8">
                <TextBlock Text="{Binding ElementName=root, Path=Title}" Style="{StaticResource HeaderText}"/>
                <TextBlock Text="  •  " Style="{StaticResource SubtleText}" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding ElementName=root, Path=Subtitle}" Style="{StaticResource SubtleText}" VerticalAlignment="Center"/>
            </StackPanel>

            <DockPanel Grid.Row="1" Margin="0,0,0,8">
                <TextBox x:Name="FilterBox" Width="220" Margin="0,0,6,0"
                         ToolTip="Type to filter rows"
                         VerticalContentAlignment="Center"
                         Height="26"
                         TextChanged="FilterBox_TextChanged"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Content="Copy Row" Margin="0,0,6,0" Click="CopyRow_Click" Height="26"/>
                    <Button Content="Export CSV" Click="ExportCsv_Click" Height="26"/>
                </StackPanel>
            </DockPanel>

            <DataGrid x:Name="Grid" Grid.Row="2" Style="{StaticResource CodeGridStyle}"/>
        </Grid>
    </Border>
</UserControl>
